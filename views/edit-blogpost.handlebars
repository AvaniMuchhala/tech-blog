{{!-- Form to update/delete user's blogpost --}}
{{log this}}

<section class="card col-10">
    <section class="card-header d-flex justfiy-content-between">
        Edit Post
    </section>

    <section class="card-body">
        <form class="d-flex flex-column">
            <label for="title">Title</label>
            {{!-- <input type="text" id="title" value={{blogpost.title}}> --}}
            <textarea id="title">
                {{blogpost.title}}
            </textarea>

            <label for="content">Content</label>
            {{!-- <input type="text" id="content" name="content" value={{blogpost.content}}> --}}
            <textarea id="content">
                {{blogpost.content}}
            </textarea>

            <input type="hidden" id="post-id" value={{blogpost.id}}>
            
            <button type="submit" id="update-post">Update Post</button>
            <button type="submit" id="delete-post">Delete</button>
        </form>
    </section>

</section>

{{!-- <script src=""></script> --}}